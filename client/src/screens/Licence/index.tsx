import React from 'react';
import { View, Text } from 'react-native';
import styles from './styles';

const Licence: React.FC = () => {
	return (
		<View>
			<Text>Essa é a pagina de Licença.</Text>
		</View>
	);
};

export default Licence;
